# Auto Reloader for Exiled Plugins

This plugin automatically monitors DLL files in the Exiled Plugins folder for changes. When a change is detected, it reloads the affected plugin without requiring a server restart. This speeds up the development and testing process by immediately applying changes.
